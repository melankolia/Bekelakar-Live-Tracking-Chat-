import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: 2,
    width: 60,
    height: 60,
    borderRadius: 40,
    borderWidth: 2,
    borderColor: 'white',
  },
});

export default styles;
